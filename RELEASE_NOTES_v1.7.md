# Zalo 汉化 v1.7

## 🎉 重大更新：H5 页面（WebView）汉化

之前钱包的 **Activity（交易记录）**、**Card（卡）** 页面全是英文，根因是这两个页面是 **H5 网页**（WebView 渲染的 HTML，资源 hook 全部够不着）。v1.7 给模块加上了 **WebView 内容注入翻译**：

- 页面加载完成后自动注入翻译脚本，遍历页面文本，按字典精确替换成中文
- 支持动态内容（下拉刷新、翻页加载的新内容也会被翻译）
- 支持输入框 placeholder 提示文字
- 字典合并了全部英文 + 越南语原文映射（**18576 条**），中越两种语言界面都覆盖
- 不碰页面数据、不注入额外请求，只做界面文字替换

## 其他

- 翻译表 12165 条、原文映射 en 9509 / vi 9360（与 v1.6 一致，无回归）
- 模拟器实测：模块加载正常、登录页全中文、无崩溃

## 安装

覆盖安装后，**强制停止 Zalo 再重开**（模块只在进程新建时注入）。然后进钱包 → Activity / Card 页面，英文应该都变中文了。
